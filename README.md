Requirements
------------
 
* Ruby 1.9 (latest version)
* bundler
 
[...]
 
Testing
-------
 
Install gem dependencies
 
    $ bundle install
 
Install cookbooks
 
    $ bundle exec berks install
 
Run tests
 
    $ bundle exec strainer test
